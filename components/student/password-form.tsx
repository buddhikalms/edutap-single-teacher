"use client";
import{useActionState,useEffect}from"react";import{toast}from"sonner";import{changeStudentPassword}from"@/app/(student-portal)/student/actions";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";
export function PasswordForm(){const[state,action,pending]=useActionState(changeStudentPassword,{ok:false,message:""});useEffect(()=>{if(state.message)(state.ok?toast.success:toast.error)(state.message)},[state]);return <form action={action} className="space-y-4"><div><Label>Current password</Label><Input name="currentPassword" type="password" required/></div><div><Label>New password</Label><Input name="password" type="password" minLength={8} required/></div><Button disabled={pending}>Change password</Button></form>}
