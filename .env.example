DATABASE_URL="mysql://root:password@localhost:3306/classcard_pro"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-secret"
PROVIDER_CREDENTIAL_KEY="replace-with-a-long-random-provider-secret"

# Optional fallback OAuth client values for Google Meet auto creation.
# Institutes can also save these encrypted from /live-classes/settings.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:3000/api/integrations/google/callback"

# Demo login after seeding:
# Email: admin@classcard.test
# Password: ClassCard@2026
