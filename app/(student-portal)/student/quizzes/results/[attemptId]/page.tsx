import{notFound}from"next/navigation";import{Badge}from"@/components/ui/badge";import{Card,CardContent}from"@/components/ui/card";import{prisma}from"@/lib/prisma";import{getStudentWebContext}from"@/lib/student-web";
export default async function QuizResult({params}:{params:Promise<{attemptId:string}>}){const{attemptId}=await params;const{studentId}=await getStudentWebContext();const a=await prisma.quizAttempt.findFirst({where:{id:attemptId,studentId},include:{quiz:true,answers:{include:{question:true}}}});if(!a)notFound();return <div className="mx-auto max-w-2xl space-y-5"><Card><CardContent className="p-8 text-center"><Badge variant={a.passed?"success":"warning"}>{a.status.toLowerCase().replaceAll("_"," ")}</Badge><h2 className="mt-4 text-3xl font-semibold">{a.quiz.title}</h2><p className="mt-4 text-5xl font-bold text-primary">{Number(a.score)}<span className="text-xl text-muted-foreground">/{Number(a.quiz.totalMarks)}</span></p><p className="mt-3 text-sm text-muted-foreground">{a.status==="SUBMITTED"?"Written answers are awaiting teacher review.":a.passed?"Passed":"Keep practicing"}</p></CardContent></Card></div>}
